@model IEnumerable<WebSales.Models.ClientIndexView>

@{
    var isValidAdmin = (Request.IsAuthenticated && User.IsInRole("Admin"));
}

@if (Model.Count() > 0)
{
    <table class="table table-bordered">
        <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">Name</th>
                @if (isValidAdmin)
                {
                    <th scope="col">Function</th>
                }
            </tr>
        </thead>
        <tbody>
            @foreach (var c in Model)
            {
                <tr>
                    <th scope="row">@c.Id</th>
                    <td>@c.Name</td>
                    @if (isValidAdmin)
                    {
                        <td>
                            <a href="#" onclick="return openEditForm(@c.Id)">Edit</a> | <a href="#" onclick="return deleteClient(@c.Id)">Delete</a>
                        </td>
                    }
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <h2>Database is empty!</h2>
}